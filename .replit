run = "cd backend && pip install -r requirements.txt && python app.py"
language = "python"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "cd backend && pip install -r requirements.txt && gunicorn -w 1 -b 0.0.0.0:5000 app:app"]
deploymentTarget = "cloudrun"
