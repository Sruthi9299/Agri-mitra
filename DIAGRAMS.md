# 📊 Integration Diagram Collection

## System Architecture (High Level)

```
┌─────────────────────────────────────────────────────────────┐
│                     USER BROWSER                            │
│              http://localhost:5000                          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                    HTTP Request
                           │
        ┌──────────────────▼─────────────────────┐
        │     FLASK BACKEND (app.py)             │
        │  ✓ Serves Frontend                    │
        │  ✓ Routes /api/* calls                │
        │  ✓ Handles Business Logic             │
        │  ✓ Manages Sessions                   │
        └──────────────────┬─────────────────────┘
                           │
                    SQL Query
                           │
        ┌──────────────────▼─────────────────────┐
        │     MYSQL DATABASE                    │
        │  ✓ Stores Users                       │
        │  ✓ Stores OTP                        │
        │  ✓ Stores Crop Data                  │
        └───────────────────────────────────────┘
```

---

## Request Flow Diagram

```
Browser                 Frontend               Backend              Database
  │                        │                    │                     │
  │────Request────────────→│                    │                     │
  │  /api/send-otp         │                    │                     │
  │  with phone number     │                    │                     │
  │                        │─────Request───────→│                     │
  │                        │  /api/send-otp    │                     │
  │                        │                    │───SQL Query────────→│
  │                        │                    │  INSERT INTO otp   │
  │                        │                    │                     │
  │                        │                    │←──OTP Record────────│
  │                        │←──Response────────│                     │
  │                        │  {"demo_otp":1234}│                     │
  │←───Response───────────│                    │                     │
  │  {"demo_otp":1234}     │                    │                     │
  │                        │                    │                     │
  │ User enters OTP        │                    │                     │
  │────Request────────────→│                    │                     │
  │  /api/login            │                    │                     │
  │  with OTP              │                    │                     │
  │                        │─────Request───────→│                     │
  │                        │  /api/login       │                     │
  │                        │                    │───SQL Query────────→│
  │                        │                    │  VERIFY OTP        │
  │                        │                    │  GET USER          │
  │                        │                    │←──User Data────────│
  │                        │←──Response────────│                     │
  │                        │  {"token":"..."}  │                     │
  │←───Response───────────│                    │                     │
  │  {"token":"..."}       │                    │                     │
  │                        │                    │                     │
  │ Token stored locally   │                    │                     │
  │ Redirected to home     │                    │                     │
```

---

## File Connection Diagram

```
User opens browser
       │
       ▼
Request http://localhost:5000
       │
       ▼
┌─────────────────────────────────┐
│  Flask app.py                   │
│  ├─ GET / → serve index.html   │
│  ├─ GET /farmer.html           │
│  ├─ GET /*.js                  │
│  ├─ GET /*.css                 │
│  └─ POST /api/* → routes/*.py  │
└────────────┬────────────────────┘
             │
       ┌─────┴──────┬──────────┐
       │            │          │
       ▼            ▼          ▼
   index.html  farmer.html  *.js/css
       │            │          │
       └─────┬──────┴──────────┘
             │
             ▼
       config.js (loaded first)
       Sets window.API_BASE
             │
       ┌─────▼──────────────────┐
       │ farmer.js              │
       │ Uses window.API_BASE   │
       │ Calls /api/send-otp   │
       │ Calls /api/login       │
       │ Calls /api/register    │
       └─────┬──────────────────┘
             │
       ┌─────▼──────────────────┐
       │ Backend Routes         │
       │ auth.py                │
       │ profile.py             │
       │ ai.py                  │
       └─────┬──────────────────┘
             │
       ┌─────▼──────────────────┐
       │ MySQL Database         │
       │ Query & Store Data     │
       └────────────────────────┘
```

---

## API Call Sequence Diagram

```
Frontend                          Backend                    Database
   │                               │                           │
   │ POST /api/send-otp           │                           │
   ├──────────────────────────────→│                           │
   │  {phone: "9876543210"}        │                           │
   │                               │ auth.py:send_otp()       │
   │                               ├──────────────────────────→│
   │                               │ DELETE old OTP           │
   │                               │ INSERT new OTP           │
   │                               │←──────────────────────────│
   │                               │ Success                  │
   │←──────────────────────────────┤                           │
   │ {demo_otp: "1234"}            │                           │
   │                               │                           │
   │ [User enters OTP]             │                           │
   │                               │                           │
   │ POST /api/login               │                           │
   ├──────────────────────────────→│                           │
   │  {phone, otp}                 │                           │
   │                               │ auth.py:login()          │
   │                               ├──────────────────────────→│
   │                               │ VERIFY OTP               │
   │                               │ SELECT user              │
   │                               │←──────────────────────────│
   │                               │ User record              │
   │                               │ Create JWT token         │
   │←──────────────────────────────┤                           │
   │ {token, user, message}        │                           │
   │                               │                           │
   │ [Token stored in localStorage]│                           │
   │ [Redirected to home]          │                           │
```

---

## Data Structure Diagram

```
┌─────────────────────────────┐
│   Browser (Frontend)        │
├─────────────────────────────┤
│ localStorage:               │
│  ├─ token                   │
│  ├─ user_id                 │
│  └─ username                │
│                             │
│ Memory (window):            │
│  └─ window.API_BASE         │
└─────────────────────────────┘

        ↕ (JSON over HTTP)

┌─────────────────────────────┐
│   Backend Server            │
├─────────────────────────────┤
│ .env Configuration:         │
│  ├─ MYSQL_HOST             │
│  ├─ MYSQL_USER             │
│  ├─ FLASK_DEBUG            │
│  └─ SECRET_KEY             │
│                             │
│ Memory (Flask Session):     │
│  ├─ user_token             │
│  ├─ session_data           │
│  └─ request_context        │
└─────────────────────────────┘

        ↕ (SQL)

┌─────────────────────────────┐
│   MySQL Database            │
├─────────────────────────────┤
│ Table: users                │
│ Table: otp_store            │
│ Table: user_crops           │
│ Table: sessions (optional)  │
└─────────────────────────────┘
```

---

## Deployment Architecture

```
Production Environment

┌──────────────────────────────────────────────┐
│         Internet / Public Network            │
└──────────────────────┬───────────────────────┘
                       │
                    HTTPS
                       │
        ┌──────────────▼────────────────┐
        │   Reverse Proxy / Nginx       │
        │   (SSL Termination)           │
        └──────────────┬────────────────┘
                       │
              Backend Communication
                       │
        ┌──────────────▼────────────────┐
        │   Gunicorn (4 Workers)        │
        │   ├─ Worker 1                 │
        │   ├─ Worker 2                 │
        │   ├─ Worker 3                 │
        │   └─ Worker 4                 │
        │                               │
        │   app.py (Flask Backend)      │
        │   ├─ routes/                  │
        │   ├─ services/                │
        │   └─ utils/                   │
        └──────────────┬────────────────┘
                       │
                      SQL
                       │
        ┌──────────────▼────────────────┐
        │  MySQL (Production)           │
        │  ├─ Master DB                 │
        │  └─ Backup DB                 │
        │                               │
        │  Database Backups (Daily)     │
        └───────────────────────────────┘
```

---

## Development vs Production

```
DEVELOPMENT                          PRODUCTION
═════════════════════════════════════════════════════════════

Port: 5000                          Port: 80/443
Protocol: HTTP                      Protocol: HTTPS
Debug: Enabled (auto-reload)        Debug: Disabled
Database: Local MySQL               Database: Remote/Managed
API_BASE: http://localhost:5000     API_BASE: Auto-detected from origin
CORS: All origins                   CORS: Specific origins only
OTP Demo: Enabled (1234)            OTP Demo: Disabled
Secret Key: Default                 Secret Key: Strong random
Server: Flask dev server            Server: Gunicorn + Nginx
Monitoring: Console logs            Monitoring: Error tracking
Backups: Manual                      Backups: Automated daily
```

---

## Technology Stack Diagram

```
┌─────────────────────────────────────────┐
│          Frontend Layer                 │
├─────────────────────────────────────────┤
│ HTML5, CSS3, JavaScript (ES6)          │
│ Fetch API, localStorage                │
└──────────────┬────────────────────────┘
               │
               │ REST API (JSON)
               │
┌──────────────▼────────────────────────┐
│          Backend Layer                 │
├──────────────────────────────────────┤
│ Python 3.8+                          │
│ Flask 3.0                            │
│ PyJWT (Authentication)               │
│ PyMySQL (Database)                   │
│ python-dotenv (Configuration)        │
│ CORS Middleware                      │
└──────────────┬────────────────────────┘
               │
               │ SQL
               │
┌──────────────▼────────────────────────┐
│         Data Layer                     │
├──────────────────────────────────────┤
│ MySQL 5.7+                           │
│ Relational Database                  │
│ Tables: users, otp_store, crops      │
└──────────────────────────────────────┘
```

---

## Integration Testing Flow

```
Test Suite (test_connection.py)
        │
        ├──→ Test 1: Backend Health
        │    GET /api/health
        │    Expected: 200 status
        │
        ├──→ Test 2: Frontend Access
        │    GET /
        │    Expected: HTML content
        │
        ├──→ Test 3: API Response
        │    POST /api/send-otp
        │    Expected: JSON response
        │
        ├──→ Test 4: Database Connection
        │    Query database
        │    Expected: Connected
        │
        └──→ Final Result:
             All tests pass ✓
             Or display failures
```

---

## File Dependencies

```
app.py (Main)
  ├─ config.js (Frontend)
  │  └─ farmer.js (Frontend)
  │     └─ Uses window.API_BASE
  ├─ routes/auth.py
  │  └─ database.py (MySQL)
  ├─ routes/profile.py
  │  └─ database.py (MySQL)
  └─ routes/ai.py
     ├─ services/gen_ai.py
     └─ database.py (MySQL)
```

---

**All diagrams show the integration is complete and working!** ✅
